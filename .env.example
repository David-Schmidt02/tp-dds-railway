# ============================================
# EJEMPLO DE VARIABLES DE ENTORNO PARA RAILWAY
# ============================================
# Copia este archivo y configura tus valores según el ambiente

# ----------------
# BACKEND
# ----------------
# Puerto en el que corre el backend (Railway asigna automáticamente, usar $PORT)
PORT=4000

# URI de conexión a MongoDB
# Para Railway, usa MongoDB Atlas o el servicio de MongoDB que tengas
# Ejemplo: mongodb+srv://usuario:password@cluster.mongodb.net/nombre_base
DB_URI=mongodb://localhost:27017/TiendaSol

# URL del frontend (para CORS)
# En Railway, será la URL de tu servicio frontend
# Ejemplo: https://tu-app-frontend.up.railway.app
FRONTEND_URL=http://localhost:5200

# ----------------
# FRONTEND
# ----------------
# URL del backend
# En Railway, será la URL de tu servicio backend
# Ejemplo: https://tu-app-backend.up.railway.app
REACT_APP_BACKEND_URL=http://localhost:4000

# ID del usuario por defecto para hacer pedidos
# Este ID debe existir en tu base de datos
REACT_APP_DEFAULT_USER_ID=6560f1a1e4b0a1b2c3d4e5f3

# ----------------
# NOTAS PARA RAILWAY:
# ----------------
# 1. En Railway, crea dos servicios separados: uno para backend y otro para frontend
# 2. Para el backend:
#    - Usa el Dockerfile en packages/backend/Dockerfile
#    - Configura DB_URI con tu instancia de MongoDB (MongoDB Atlas recomendado)
#    - Railway asigna automáticamente el puerto, pero puedes usar PORT=4000 como fallback
# 3. Para el frontend:
#    - Usa el Dockerfile en packages/frontend/Dockerfile
#    - Configura REACT_APP_BACKEND_URL con la URL de tu backend de Railway
# 4. NO uses el servicio mongo del docker-compose en Railway
# 5. Asegúrate de que el REACT_APP_DEFAULT_USER_ID exista en tu base de datos
